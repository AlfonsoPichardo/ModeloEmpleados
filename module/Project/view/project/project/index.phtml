<?php
// module/Album/view/album/album/index.phtml:

$title = 'Project';
$this->headTitle($title);
?>
<h1><?= $this->escapeHtml($title) ?></h1>
<p>
    <a href="<?= $this->url('project', ['action' => 'add']) ?>">Add new Project</a>
</p>

<table class="table">
<tr>
    <th>Id</th>
    <th>Title</th>
    <th>Start Date</th>
    <th>Complete</th>
    <th>Actions</th>
</tr>
<?php foreach ($project as $projects) : ?>
    <tr>
        <td><?= $this->escapeHtml($projects->id) ?></td>
        <td><?= $this->escapeHtml($projects->title) ?></td>
        <td><?= $this->escapeHtml($projects->st_date) ?></td>
        <td><?= $this->escapeHtml($projects->complete) ?></td>
        <td>
            <a href="<?= $this->url('project', ['action' => 'edit', 'id' => $project->id]) ?>">Edit</a>
            <a href="<?= $this->url('project', ['action' => 'delete', 'id' => $project->id]) ?>">Delete</a>
        </td>
    </tr>
<?php endforeach; ?>
</table>